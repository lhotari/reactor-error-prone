subprojects {
    apply plugin: 'java'

    group = 'com.github.lhotari.reactor-error-prone'
    version = '0.0.1-SNAPSHOT'
    sourceCompatibility = targetCompatibility = '8'

    repositories {
        jcenter()
    }
}
